# DataAgentBench: Retail Domain Schema

*This domain (`retail`) covers cross-database scenarios where sales info (CRM, PoS systems, Pipeline data) traverses disconnected tools like in the `query_crmarenapro` and ecommerce pipelines.*

# Corrections Log (KB v3)

This document is the agent's interaction memory (Layer 6). It stores a running log of past agent failures and the correct approaches so the agent does not repeat mistakes.

## Expected Format
Each entry must strictly follow this format:
`[query that failed] → [what was wrong] → [correct approach]`

---
## Recorded Corrections

